name: Diag
on:
  workflow_dispatch:

jobs:
  diag:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy
          rustflags: ""

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Build the lib tests
        working-directory: src-tauri
        run: cargo test --lib --no-run

      - name: Create the comctl32 v6 manifests (same as ci.yml)
        working-directory: src-tauri
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
          $manifest = @'
          <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
          <assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
            <dependency>
              <dependentAssembly>
                <assemblyIdentity type="win32" name="Microsoft.Windows.Common-Controls" version="6.0.0.0" processorArchitecture="*" publicKeyToken="6595b64144ccf1df" language="*"/>
              </dependentAssembly>
            </dependency>
          </assembly>
          '@
          Get-ChildItem target/debug/deps/*.exe | ForEach-Object {
            Set-Content -Path "$($_.FullName).manifest" -Value $manifest.TrimStart()
            Write-Host "wrote $($_.FullName).manifest"
          }
          Get-ChildItem target/debug/deps/*.manifest | ForEach-Object {
            Write-Host "--- $($_.Name) ($($_.Length) bytes)"
            Get-Content $_.FullName -TotalCount 3 | Out-String | Write-Host
          }

      - name: Run the test exe and capture every diagnostic
        working-directory: src-tauri
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
          $exe = Get-ChildItem target/debug/deps/persea_desktop_lib-*.exe | Select-Object -First 1
          Write-Host "exe: $($exe.FullName)"
          & $exe.FullName 2>&1 | Out-String | Write-Host
          Write-Host "exit code: $LASTEXITCODE"
          Start-Sleep -Seconds 8

          Write-Host "=== WER ReportQueue ==="
          Get-ChildItem 'C:\ProgramData\Microsoft\Windows\WER\ReportQueue' -ErrorAction SilentlyContinue |
            Where-Object Name -match 'persea' | ForEach-Object {
              Write-Host "queue: $($_.FullName)"
              Get-ChildItem $_.FullName | ForEach-Object {
                Write-Host "--- $($_.Name)"
                Get-Content $_.FullName -TotalCount 60 -ErrorAction SilentlyContinue | Out-String | Write-Host
              }
            }

          Write-Host "=== Application event log (unfiltered, last 15) ==="
          Get-WinEvent -LogName Application -MaxEvents 15 -ErrorAction SilentlyContinue |
            ForEach-Object { Write-Host "---"; Write-Host "$($_.ProviderName) [$($_.Id)]"; Write-Host $_.Message }

          Write-Host "=== embedded manifest check ==="
          $bytes = [System.IO.File]::ReadAllBytes($exe.FullName)
          $text = [System.Text.Encoding]::ASCII.GetString($bytes)
          Write-Host ("has <assembly marker: " + ($text -match '<assembly').ToString())
          Write-Host ("has Common-Controls: " + ($text -match 'Common-Controls').ToString())
          exit 0

      - name: Audit the import table against the runner's DLLs
        working-directory: src-tauri
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
          python -m pip install --quiet pefile
          $py = @'
          import pefile, os
          deps_dir = os.path.join(os.getcwd(), "target", "debug", "deps")
          exe = [os.path.join(deps_dir, f) for f in os.listdir(deps_dir)
                 if f.startswith("persea_desktop_lib") and f.endswith(".exe")][0]
          pe = pefile.PE(exe)
          windir = os.environ.get("WINDIR", "C:\\Windows")
          search = [os.path.dirname(exe), os.path.join(windir, "System32"),
                    os.path.join(windir, "SysWOW64"), windir]
          search += [p for p in os.environ.get("PATH", "").split(";") if p]
          problems = []
          for entry in getattr(pe, "DIRECTORY_ENTRY_IMPORT", []):
              dll = entry.dll.decode()
              cands = [os.path.join(d, dll) for d in search]
              found = next((c for c in cands if os.path.exists(c)), None)
              if not found:
                  problems.append(dll + ": DLL NOT FOUND in search path")
                  continue
              try:
                  dpe = pefile.PE(found, fast_load=True)
                  dpe.parse_data_directories(directories=[pefile.DIRECTORY_ENTRY["IMAGE_DIRECTORY_ENTRY_EXPORT"]])
                  exports = set()
                  if hasattr(dpe, "DIRECTORY_ENTRY_EXPORT"):
                      for sym in dpe.DIRECTORY_ENTRY_EXPORT.symbols:
                          if sym.name:
                              exports.add(sym.name.decode())
                  for imp in entry.imports:
                      if imp.name and imp.name.decode() not in exports:
                          problems.append(dll + ": MISSING EXPORT " + imp.name.decode())
              except Exception as e:
                  problems.append(dll + ": parse error " + str(e))
          print("PROBLEMS:" if problems else "NO MISSING IMPORTS")
          for pr in problems:
              print("  ", pr)
          '@
          Set-Content -Path import_audit.py -Value $py
          python import_audit.py

  # Linux diag: reproduce the audit.yml "Provision the test persea server"
  # step with full stderr (provision-server.sh hides cargo output) and
  # runner-environment details. Temporary: remove once the provisioning
  # failure is diagnosed.
  diag-linux:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy
          rustflags: ""

      - name: Runner environment
        run: |
          echo "=== toolchains ==="
          which -a cargo rustc || true
          rustc -V; cargo -V
          rustup toolchain list || true
          echo "RUSTUP_TOOLCHAIN=${RUSTUP_TOOLCHAIN:-unset}"
          echo "=== resources ==="
          df -h / /tmp /mnt 2>/dev/null || df -h
          free -h
          nproc

      - name: Clone the server and build it with visible stderr
        run: |
          git clone --no-checkout https://github.com/persea-grove/persea.git /tmp/persea-probe
          git -C /tmp/persea-probe checkout 43215ab
          df -h /tmp
          set +e
          cargo build --release --manifest-path /tmp/persea-probe/Cargo.toml 2>&1 | tail -60
          code=$?
          echo "cargo build exit code: $code"
          df -h /tmp
          exit 0
