{
  "name": "persea-desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Persea Desktop shell tooling: eslint and node --test for the classic-script shell assets (no bundler yet).",
  "scripts": {
    "lint": "eslint shell/",
    "test": "node --test \"shell/lib/*.test.js\""
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "globals": "^16.5.0"
  }
}